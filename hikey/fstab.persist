# Android fstab file.
#<src>                                                  <mnt_point>         <type>    <mnt_flags and options>                               <fs_mgr_flags>
# The filesystem that contains the filesystem checker binary (typically /system) cannot
# specify MF_CHECK, and must come before any filesystems that do specify MF_CHECK

/dev/block/platform/soc/f723d000.dwmmc0/by-name/nvme        /mnt/vendor/persist ext4      nosuid,nodev,noatime,barrier=1    wait,first_stage_mount
#/dev/block/platform/soc/f723d000.dwmmc0/by-name/nvme        /mnt/vendor/persist ext4      nosuid,nodev,noatime,data=ordered,barrier=1    wait,first_stage_mount
